<UserControl x:Class="WinForge.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock x:Name="TitleText" Style="{StaticResource PageTitle}" />
            <TextBlock x:Name="SubtitleText" Style="{StaticResource PageSubtitle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,18">
            <StackPanel Orientation="Horizontal">
                <Button Content="Select all in this page" Style="{StaticResource SecondaryButton}" Click="SelectAllButton_Click" />
                <Button Content="Clear this page" Style="{StaticResource SecondaryButton}" Click="ClearButton_Click" />
                <TextBlock Text="Changes are applied from the Optimise page." Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="8,0,0,10" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="OptionsPanel" />
        </ScrollViewer>
    </Grid>
</UserControl>
