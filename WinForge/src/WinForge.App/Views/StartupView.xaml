<UserControl x:Class="WinForge.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="115" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="Startup" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Review startup entries and toggle them on or off inside WinForge." Style="{StaticResource PageSubtitle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="10" Margin="0,0,0,10">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" VerticalAlignment="Center">
                    <TextBlock Text="Search" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,8" />
                    <TextBox x:Name="SearchBox" Width="330" Height="32" Background="#FF0D1322" Foreground="{StaticResource InkBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="8,5" TextChanged="SearchBox_TextChanged" Margin="0,0,12,8" />
                    <Button Content="Refresh" Style="{StaticResource PrimaryButton}" Padding="12,7" MinHeight="32" Click="RefreshButton_Click" />
                    <Button Content="Apply" Style="{StaticResource SecondaryButton}" Padding="12,7" MinHeight="32" Click="ApplyButton_Click" />
                    <Button Content="Enable visible" Style="{StaticResource SecondaryButton}" Padding="12,7" MinHeight="32" Click="EnableVisibleButton_Click" />
                    <Button Content="Disable visible" Style="{StaticResource SecondaryButton}" Padding="12,7" MinHeight="32" Click="DisableVisibleButton_Click" />
                    <Button Content="Startup folder" Style="{StaticResource SecondaryButton}" Padding="12,7" MinHeight="32" Click="OpenStartupFolderButton_Click" />
                    <Button Content="Report" Style="{StaticResource SecondaryButton}" Padding="12,7" MinHeight="32" Click="ReportButton_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="#FF0D1322" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ItemsPanel" />
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="10" Margin="0,10,0,0">
            <TextBox x:Name="ReportBox" Background="#FF0D1322" Foreground="{StaticResource InkBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="10" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" />
        </Border>
    </Grid>
</UserControl>
